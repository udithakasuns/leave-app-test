import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    scrollViewContainer: {
        width: '100%',
        height: '100%',
    },
    flatListContainer: { flexGrow: 0 },
    columnWrapperStyle: {
        overflow: 'visible',
    },
});
