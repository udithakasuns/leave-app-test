import React from 'react';
import { View, Text } from 'react-native';
import { NotificationsScreensProps } from 'src/navigators/types';

const Notifications: React.FC<NotificationsScreensProps> = () => (
    <View>
        <Text>All Notifications</Text>
    </View>
);

export default Notifications;
